# Required
GEMINI_API_KEY=your-gemini-api-key

# Optional — Voice messaging (Deepgram Nova-3 speech-to-text)
DEEPGRAM_API_KEY=

# Optional — Nutrition search (USDA FoodData Central)
USDA_API_KEY=

# Optional — Basic Auth (both must be set to enable)
AUTH_USERNAME=
AUTH_PASSWORD=

# Optional — defaults shown
PORT=3000
DB_PATH=/data/workout.db
CHROMA_HOST=chromadb
CHROMA_PORT=8000
