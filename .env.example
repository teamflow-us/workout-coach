# =============================================================================
#  Workout Coach — Environment Variables
# =============================================================================

# Required
GEMINI_API_KEY=your-gemini-api-key
DATABASE_URL=postgres://postgres:password@your-supabase-host:5432/workout_coach

# Optional — Auth (point to your Supabase GoTrue instance)
# JWT_SECRET must match GoTrue's GOTRUE_JWT_SECRET (default: 01234567890123456789012345678901)
JWT_SECRET=
GOTRUE_URL=

# Optional — External APIs
DEEPGRAM_API_KEY=
USDA_API_KEY=

# Optional — Port override (default: 3000)
# APP_PORT=3000
