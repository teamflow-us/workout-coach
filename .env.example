# =============================================================================
#  Workout Coach — Environment Variables
#
#  Quick start:
#    1. Copy this file:  cp .env.example .env
#    2. Fill in the REQUIRED values below
#    3. Generate Supabase keys:  node scripts/generate-supabase-keys.mjs
#    4. Paste the output into this file
#    5. docker compose up -d
# =============================================================================

# ---------------------------------------------------------------------------
#  REQUIRED — Application
# ---------------------------------------------------------------------------
GEMINI_API_KEY=your-gemini-api-key

# ---------------------------------------------------------------------------
#  REQUIRED — Database & Auth
# ---------------------------------------------------------------------------
POSTGRES_PASSWORD=your-strong-db-password
# URL-encoded version of POSTGRES_PASSWORD (encode special chars: ! → %21, # → %23, etc.)
POSTGRES_PASSWORD_URLENCODED=your-strong-db-password
JWT_SECRET=your-jwt-secret-at-least-32-characters-long

# ---------------------------------------------------------------------------
#  REQUIRED — Supabase JWT Keys
#  Generate with:  node scripts/generate-supabase-keys.mjs
# ---------------------------------------------------------------------------
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# ---------------------------------------------------------------------------
#  Optional — GoTrue auth
# ---------------------------------------------------------------------------
GOTRUE_OPERATOR_TOKEN=super-secret-operator-token
SITE_URL=http://localhost:3000
API_EXTERNAL_URL=http://localhost:8000
DISABLE_SIGNUP=false

# ---------------------------------------------------------------------------
#  Optional — External APIs
# ---------------------------------------------------------------------------
DEEPGRAM_API_KEY=
USDA_API_KEY=

# ---------------------------------------------------------------------------
#  Optional — Port overrides (defaults shown)
# ---------------------------------------------------------------------------
# APP_PORT=3000
# KONG_HTTP_PORT=8000
# KONG_HTTPS_PORT=8443
# STUDIO_PORT=54322
# POSTGRES_PORT=5432
